# RAG-System
A production-grade multimodal RAG system with hybrid retrieval (BM25 + dense vectors + RRF), Cohere reranking, and Claude-powered generation — enabling accurate, citation-grounded question answering over PDFs, DOCX, images, and text files with a streaming FastAPI backend and Streamlit UI.
